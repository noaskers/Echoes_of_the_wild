// River and waterfall generation disabled: this is now a stub to prevent any water/river features from being created or referenced.
using UnityEngine;
public class RiverGenerator : MonoBehaviour
{
    // All river, waterfall, and pond generation is disabled.
    // This stub exists only to prevent errors in scripts that reference RiverGenerator.
    public float riverWidth = 0f;
    public float riverValleyDepth = 0f;
    public float waterLevel = 0f;
    public void GenerateRiver() { /* intentionally empty */ }
}
